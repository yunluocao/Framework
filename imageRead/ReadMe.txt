1.This tool could compare the image,if it's different it will give differnt part
2.This tool also could help the user to cut the screenshot, as some screen some part UI are fixed, 
we only need to compare fixed part whether is changed.
3.This tool could parse the image into string type, then we could use it to transfer by ssh.